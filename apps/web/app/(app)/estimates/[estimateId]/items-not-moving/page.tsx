import { WorkspacePlaceholder } from "@/components/estimates/workspace-placeholder";

export default function EstimateItemsNotMovingPage() {
  return (
    <WorkspacePlaceholder
      title="Items not Moving"
      description="Track excluded inventory items at estimate scope in a later phase."
    />
  );
}
