import { WorkspacePlaceholder } from "@/components/estimates/workspace-placeholder";

export default function EstimateTasksPage() {
  return (
    <WorkspacePlaceholder
      title="Tasks List"
      description="Estimate-specific task tracking and assignment will be added in a later phase."
    />
  );
}
